<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">
    <context:annotation-config />
    <context:component-scan base-package="foo.bar"/>

    <bean id="poem" class="foo.bar.Poem"></bean>

    <bean id="vasya" class="foo.bar.Juggler">
        <constructor-arg name="countOfBeanbags" value="15" />
        <constructor-arg name="poem" ref="poem" />
    </bean>

    <!-- внедрение внутренних компонентов -->
    <bean id="kolya" class="foo.bar.Juggler">
        <constructor-arg name="countOfBeanbags" value="15" />
        <property name="poem">
            <bean class="foo.bar.Poem"></bean>
        </property>
    </bean>

    <!-- свяывание с помощью пространства имен p -->
    <bean id="dima" class="foo.bar.Juggler"
          p:countOfBeanbags="15"
          p:poem-ref="poem"
    />

    <bean id="guitar" class="foo.bar.Guitar"></bean>
    <bean id="bass" class="foo.bar.Bass"></bean>
    <bean id="drums" class="foo.bar.Drums"></bean>

    <!-- внедрение коллекций -->
    <bean id="d" class="foo.bar.OneManBand">
        <property name="instruments">
            <list>
                <bean class="foo.bar.Guitar"></bean>
                <bean class="foo.bar.Bass"></bean>
                <bean class="foo.bar.Drums"></bean>
            </list>
        </property>
    </bean>

    <!-- внедрение отображений -->
    <bean id="m" class="foo.bar.OneManBandWithMap">
        <property name="instruments">
            <map>
                <entry key="guitar" value-ref="guitar" />
                <entry key="bass" value-ref="bass"/>
                <entry key="drums" value-ref="drums"/>
            </map>
        </property>
    </bean>

    <!-- внедрение пустого значения -->
    <bean id="none" class="foo.bar.OneManBandWithMap">
        <property name="instruments"><null/></property>
    </bean>

</beans>